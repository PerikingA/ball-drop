/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export'
  };/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/ball-drop',
  assetPrefix: '/ball-drop',
};

export default nextConfig;

  
  export default nextConfig;
  
